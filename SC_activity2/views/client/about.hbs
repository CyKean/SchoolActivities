<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>About</title>
   <script src="https://cdn.tailwindcss.com"></script>

</head>
<body>

    <div class="flex justify-center items-center content-center w-screen h-screen p-3">
        <div class="border border-green-500 p-3 rounded-md flex flex-col justify-center items-center">
            <div class="flex w-full m-2">
                <a href="/home" class="flex text-left text-green-700"><-back-</a>
            </div>
            <div class="flex flex-col justify-center content-center border border-green-500 rounded-md mb-2">
                {{#if notification}}
                    <p style="color: red;">{{notification}}</p>
                {{/if}}
              <h3 class="text-center p-3 text-xl">About <a href="#" class="text-green-700 font-semibold">{{user.username}}</a></h3>

                {{#if about}}
                    <div class="m-3 border border-green-500 rounded-md flex justify-center content-center">
                        <p class="text-center">{{about.content}}</p>
                    </div>
                    <div class="flex justify-evenly content-evenly m-3 p-2 ">
                        <form action="/about/edit" method="get">
                        <button type="submit" class="bg-green-500 text-gray-200 p-2 rounded-md hover:bg-green-400 hover:text-white">Edit</button>
                        </form>
                        <form action="/about/delete" method="POST" onsubmit="return confirm('Are you sure you want to delete your About information?');">
                            <button type="submit" class="bg-red-500 text-gray-200 p-2 rounded-md hover:bg-red-400 hover:text-white">Delete</button>
                        </form>
                {{else}}
                    </div>
                    <p class="p-3 text-center">You haven't added any About information yet.</p>
                     <form action="/about/add" method="get">
                        <button type="submit" class="bg-green-500 text-gray-200 p-2 rounded-md hover:bg-green-400 hover:text-white">Add About</button>
                    </form>
            </div>
                
                {{/if}} 
            
        </div>
    </div>
</body>
</html>
